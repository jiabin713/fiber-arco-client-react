import { Spin } from '@arco-design/web-react';

const FallbackCompent = () => (
  <div className='flex content-center items-center justify-center w-full h-full '>
    <Spin />
  </div>
);

export default FallbackCompent;
